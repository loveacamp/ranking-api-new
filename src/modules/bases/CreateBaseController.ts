import { Request, Response } from "express";

export default class CreateBaseController {
    // async execute(request: Request, response: Response): Promise<Response> {
    //     const { name, term, city, church } = request.body;
    // }
}
